{
  "dependencies": {
    "@clerk/chrome-extension": "^3.1.35",
    "convex": "^1.41.0",
    "jspdf": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build:clerk": "node scripts/build-clerk.mjs",
    "clean": "rm -f cloud-sync.js && rm -rf convex/_generated"
  }
}
